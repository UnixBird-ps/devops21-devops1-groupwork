
name: Prevent change

on:
  push:
    branches: ps-test*
  pull_request:
    branches: ps-test*
  delete:
    branches: ps-test*

jobs:
  prevent_change:
    name: Stop if not owner
    runs-on: ubuntu-latest
    steps:
    - name: Bailing out
      if: github.actor != github.repository_owner
      run: exit 1
    - name: Continue
      run: exit 0
